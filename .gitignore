# Secrets — NEVER commit. Holds the Resend API key.
.env
.env.*
!.env.example

# Local QA artifacts (headless-browser screenshots for design review)
.qa/

# Source archive for the gallery shoot — originals live outside git
images/gallery.zip

# macOS
.DS_Store

# FUSE filesystem leftovers (stale "deleted but in use" files)
.fuse_hidden*

# Editor / OS cruft
Thumbs.db
*.swp
*~

# Node (Vercel remnants — safe to remove the api/ folder and package.json too)
node_modules/
